describe ("The calculate bill factory function", function() {

    it ("should give you the total of the calls and sms string that is entered", function () {

    //   let billTotals = CalculatebillTotal();

     //assert.equal(billTotal("sms", "call", 3.50));
     assert.equal(CalculatebillTotal("sms, call"), 3.50);

    })
})
